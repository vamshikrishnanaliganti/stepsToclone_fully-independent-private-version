mkdir terraform-aws-rds (directory name)
cd terraform-aws-rds 
git clone https://github.com/CloudTechDevOps/terraform-aws-rds.git temp

mv temp/* temp/.* . 2>/dev/null
rm -rf temp .git

git init
git add .
git commit -m "added script "


git remote add origin https://github.com/vamshikrishnanaliganti/project-1-maven-jenkins-CICD-docker-eks-.git
git branch -M main
git push -u origin main

